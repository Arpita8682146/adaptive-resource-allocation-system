from process import Process
from resource_manager import ResourceManager

# Create Resource Manager
rm = ResourceManager()

# Create processes
p1 = Process("P1", 10, 200, 1)
p2 = Process("P2", 15, 300, 2)
p3 = Process("P3", 20, 400, 3)

processes = [p1, p2, p3]

# Allocate resources statically
for p in processes:
    rm.allocate_static(p)

# Display process info
for p in processes:
    p.display_info()

# Display remaining system resources
rm.display_system_status()
from process import Process
from resource_manager import ResourceManager

# Create Resource Manager
rm = ResourceManager()

# Create processes
p1 = Process("P1", 10, 200, 1)
p2 = Process("P2", 15, 300, 2)
p3 = Process("P3", 20, 400, 3)

processes = [p1, p2, p3]

# Allocate resources statically
for p in processes:
    rm.allocate_static(p)

# Display process info
for p in processes:
    p.display_info()

# Display remaining system resources
rm.display_system_status()
from process import Process
from resource_manager import ResourceManager

# Create Resource Manager
rm = ResourceManager()

# Create processes
p1 = Process("P1", 10, 200, 1)
p2 = Process("P2", 15, 300, 2)
p3 = Process("P3", 20, 400, 3)

processes = [p1, p2, p3]

# Allocate resources statically
for p in processes:
    rm.allocate_static(p)

# Display process info
for p in processes:
    p.display_info()

# Display remaining system resources
rm.display_system_status()
from process import Process
from resource_manager import ResourceManager

# Create Resource Manager
rm = ResourceManager()

# Create processes
p1 = Process("P1", 10, 200, 1)
p2 = Process("P2", 15, 300, 2)
p3 = Process("P3", 20, 400, 3)

processes = [p1, p2, p3]

# Allocate resources statically
for p in processes:
    rm.allocate_static(p)

# Display process info
for p in processes:
    p.display_info()

# Display remaining system resources
rm.display_system_status()
from process import Process
from resource_manager import ResourceManager

# Create Resource Manager
rm = ResourceManager()

# Create processes
p1 = Process("P1", 10, 200, 1)
p2 = Process("P2", 15, 300, 2)
p3 = Process("P3", 20, 400, 3)

processes = [p1, p2, p3]

# Allocate resources statically
for p in processes:
    rm.allocate_static(p)

# Display process info
for p in processes:
    p.display_info()

# Display remaining system resources
rm.display_system_status()
from process import Process
from resource_manager import ResourceManager

# Create Resource Manager
rm = ResourceManager()

# Create processes
p1 = Process("P1", 10, 200, 1)
p2 = Process("P2", 15, 300, 2)
p3 = Process("P3", 20, 400, 3)

processes = [p1, p2, p3]

# Allocate resources statically
for p in processes:
    rm.allocate_static(p)

# Display process info
for p in processes:
    p.display_info()

# Display remaining system resources
rm.display_system_status()
from process import Process
from resource_manager import ResourceManager

# Create Resource Manager
rm = ResourceManager()

# Create processes
p1 = Process("P1", 10, 200, 1)
p2 = Process("P2", 15, 300, 2)
p3 = Process("P3", 20, 400, 3)

processes = [p1, p2, p3]

# Allocate resources statically
for p in processes:
    rm.allocate_static(p)

# Display process info
for p in processes:
    p.display_info()

# Display remaining system resources
rm.display_system_status()